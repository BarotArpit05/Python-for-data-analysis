a='Arpit' #can define string using Single quote
b="Arpit" #can define string using double quote
c='''Arpit''' # can define string using triple quote

#String Slicing 

nameshort=a[0:3] #so it will start from index 0 and will go till the index 3 but will exclude index3
print(nameshort)